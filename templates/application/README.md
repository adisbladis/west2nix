# West2nix example

This directory serves as the workspace root and is initialized with:
``` bash
$ west init -l app
$ cd app
$ west update
$ wes build
```

## Updating west2nix.toml
```
$ west2nix
```
